import { ParagrapheDirective } from './paragraphe.directive';

describe('ParagrapheDirective', () => {
  it('should create an instance', () => {
    const directive = new ParagrapheDirective();
    expect(directive).toBeTruthy();
  });
});
